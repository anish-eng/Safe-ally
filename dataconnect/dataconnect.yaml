specVersion: "v1"
serviceId: "safehaven"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "safehaven-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
