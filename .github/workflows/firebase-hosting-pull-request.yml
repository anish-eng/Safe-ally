# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# # name: Deploy to Firebase Hosting on PR
# # on: pull_request
# # permissions:
# #   checks: write
# #   contents: read
# #   pull-requests: write
# # jobs:
# #   build_and_preview:
# #     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
# #     runs-on: ubuntu-latest
# #     steps:
# #       - uses: actions/checkout@v4
# #       - run: npm ci && npm run build
# #       - env:
# #           CI: false
# #       - uses: FirebaseExtended/action-hosting-deploy@v0
# #         with:
# #           repoToken: ${{ secrets.GITHUB_TOKEN }}
# #           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOMESTICABUSE_12902 }}
# #           projectId: domesticabuse-12902


# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# # name: Deploy to Firebase Hosting on PR

# # on:
# #   pull_request:

# # permissions:
# #   checks: write
# #   contents: read
# #   pull-requests: write

# # jobs:
# #   build_and_preview:
# #     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
# #     runs-on: ubuntu-latest
# #     steps:
# #       - uses: actions/checkout@v4

# #       - name: Install & Build (allow CRA warnings)
# #         run: |
# #           npm ci
# #           npm run build
# #         env:
# #           CI: false

# #       - uses: FirebaseExtended/action-hosting-deploy@v0
# #         with:
# #           repoToken: ${{ secrets.GITHUB_TOKEN }}
# #           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOMESTICABUSE_12902 }}
# #           projectId: domesticabuse-12902



# # .github/workflows/firebase-hosting-pull-request.yml
# # .github/workflows/firebase-hosting-pull-request.yml
# name: Deploy to Firebase Hosting on PR

# on:
#   pull_request:

# permissions:
#   checks: write
#   contents: read
#   pull-requests: write

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   build_and_preview:
#     # Only run for PRs from this repo (keeps secrets safe from forks)
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install & Build (ignore lint + TS type errors)
#         run: |
#           npm ci
#           npm run build
#         env:
#           CI: false
#           DISABLE_ESLINT_PLUGIN: true
#           TSC_COMPILE_ON_ERROR: true

#       - name: Deploy Preview
#         uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOMESTICABUSE_12902 }}
#           projectId: domesticabuse-12902




name: Deploy to Firebase Hosting on PR
on: pull_request

permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 👇 Add this block
      - name: Create .env.production from GitHub Secrets
        run: |
          cat > .env.production <<'EOF'
          REACT_APP_FB_API_KEY=${{ secrets.REACT_APP_FB_API_KEY }}
          REACT_APP_FB_AUTH_DOMAIN=${{ secrets.REACT_APP_FB_AUTH_DOMAIN }}
          REACT_APP_FB_PROJECT_ID=${{ secrets.REACT_APP_FB_PROJECT_ID }}
          REACT_APP_FB_STORAGE_BUCKET=${{ secrets.REACT_APP_FB_STORAGE_BUCKET }}
          REACT_APP_FB_MESSAGING_SENDER_ID=${{ secrets.REACT_APP_FB_MESSAGING_SENDER_ID }}
          REACT_APP_FB_APP_ID=${{ secrets.REACT_APP_FB_APP_ID }}
          REACT_APP_FB_MEASUREMENT_ID=${{ secrets.REACT_APP_FB_MEASUREMENT_ID }}
          EOF

      - run: npm ci
      - run: CI=false npm run build
      - run: ls -la build || true

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DOMESTICABUSE_12902 }}
          projectId: domesticabuse-12902
      



